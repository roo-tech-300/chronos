# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies / Build Outputs
node_modules
dist
dist-ssr
*.local

# Tauri / Rust Build Output
src-tauri/target/
# Tauri auto-generated schemas (regenerated on every build)
src-tauri/gen/schemas/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
opencode.json

# Node Bridge runtime artifacts (local SQLite db + scanner captures)
# IMPORTANT: node-bridge/.exec/bmp/ holds raw fingerprint bitmap images.
# Per biometric privacy policy these must NEVER be committed.
node-bridge/.db/
node-bridge/.exec/
node-bridge/.env
